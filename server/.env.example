# Скопируйте в .env и заполните своими значениями.
# Запуск с этим файлом:  set -a; . ./.env; set +a; node server.js

# Пароль резервной админки по адресу /admin (обязательно).
ADMIN_PASSWORD=ПРИДУМАЙТЕ_СЛОЖНЫЙ_ПАРОЛЬ

# Токен бота из BotFather. Если задан — подпись Telegram initData проверяется.
TELEGRAM_BOT_TOKEN=

# Telegram ID администратора (кому доступна админка в мини-аппе).
ADMIN_TG_ID=8211351879

# Публичный адрес сервиса (поддомен, чтобы не трогать основной сайт/VPN).
# Этот адрес зашивается в QR-код привязки.
PUBLIC_BASE_URL=https://project.alfa-vpn.ru

# Порт локального Node-процесса (за nginx). Возьмите свободный, НЕ занятый VPN/старым ALFA SMS.
PORT=8090

# Номер получателя всех платёжных SMS + источник «Операция отклонена» /
# «Оплата не произведена» (по умолчанию 7878).
RECIPIENT_NUMBER=7878

# Номер, с которого приходят «символ»/«успешно» (туда же уходит «Ок»). По умолч. 8464.
SIGNAL_NUMBER=8464

# Часовой пояс сервера — по нему считается расписание (окна). Задайте пояс
# ваших пользователей, иначе окна сместятся. Пример: Europe/Moscow.
TZ=Europe/Moscow

# Логин резервной админки (необязательно).
ADMIN_USER=admin

# Секрет для вебхука «сигнала» из MacroDroid. Если не задать — сгенерируется
# автоматически из ADMIN_PASSWORD и будет напечатан в логах при старте.
# SIGNAL_SECRET=
