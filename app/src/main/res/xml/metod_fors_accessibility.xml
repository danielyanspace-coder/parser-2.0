<?xml version="1.0" encoding="utf-8"?>
<!--
  "Метод Форс" accessibility service. It observes and drives the Beeline app's
  UI (find labels, tap buttons, fill fields) to make a card transfer at two exact
  Moscow-time moments each hour. It is not restricted to one package here so the
  driven app can be re-targeted from the server without an APK rebuild.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:notificationTimeout="100"
    android:description="@string/mf_service_description" />
